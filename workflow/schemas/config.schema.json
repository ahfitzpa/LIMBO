{
  "type": "object",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "configuration",
  "description": "This is the format used for configuring the randomizer",
  "examples": [{"num_samples":90,"max_genotypes":100}],
  "properties": {
    "num_samples": {
      "type": "integer",
      "default": 40,
      "description": "Number of samples to generate.",
      "examples": [100]
    },
    "max_genotypes": {
      "type": "integer",
      "default": 20,
      "description": "Maximum number of genotypes to pick for each sample.\nEach sample will have between 2 and this number of max_genotypes in its mixture.",
      "examples": [100]
    },
    "genome_path": {
      "type": "string",
      "default": "resource/genomes",
      "description": "Path containing a collection of .fasta files with the genomes to use for the samples.",
      "examples": [ "genonomes/" ]
    }
  }
}

  
